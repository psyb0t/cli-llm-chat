# Direct dependencies and security floors, not a lockfile.
#
# This used to be a `pip freeze` dump, which pinned torch's whole CUDA
# transitive closure (nvidia-*-cu12, triton) to the versions that shipped with
# torch 2.3.1. Those pins made it impossible to move torch without hand-editing
# a dozen unrelated lines, so they are gone -- torch pulls in the CUDA wheels
# matching whatever torch version resolves.
#
# Lower bounds on the transitive packages below are deliberate: they are the
# first versions clearing published advisories, kept here so a resolver cannot
# quietly pick a vulnerable one.

# Directly imported by the application
torch>=2.13.0
transformers>=5.5.0
peft>=0.18.0
python-telegram-bot>=21.4

# Needed by transformers/torch at runtime for the features this app uses
accelerate>=1.0.0
bitsandbytes>=0.43.1
protobuf>=5.29.6
sentencepiece>=0.2.1

# Security floors on transitive dependencies
certifi>=2024.7.4
filelock>=3.20.3
idna>=3.15
Jinja2>=3.1.6
requests>=2.33.0
urllib3>=2.7.0
